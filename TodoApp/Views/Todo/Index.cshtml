@model IEnumerable<TodoApp.Models.TodoItem>

@{
    ViewData["Title"] = "待辦事項清單";
}

<div class="container mt-4">
    <div class="row mb-4">
        <div class="col-md-12">
            <h1 class="display-4">📝 待辦事項清單</h1>
            <p class="lead">管理你的待辦事項</p>
        </div>
    </div>

    <div class="row mb-3">
        <div class="col-md-12">
            <a asp-action="Create" class="btn btn-primary">
                <i class="bi bi-plus-circle"></i> 新增待辦事項
            </a>
        </div>
    </div>

    <div class="row">
        <div class="col-md-12">
            @if (!Model.Any())
            {
                <div class="alert alert-info">
                    目前沒有待辦事項。<a asp-action="Create">立即新增</a>一個吧！
                </div>
            }
            else
            {
                <div class="card">
                    <div class="card-body">
                        <table class="table table-hover">
                            <thead>
                                <tr>
                                    <th style="width: 5%;">狀態</th>
                                    <th style="width: 25%;">標題</th>
                                    <th style="width: 35%;">描述</th>
                                    <th style="width: 15%;">建立時間</th>
                                    <th style="width: 20%;">操作</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr class="@(item.IsCompleted ? "table-success" : "")">
                                        <td>
                                            <form asp-action="ToggleComplete" asp-route-id="@item.Id" method="post" class="d-inline">
                                                @Html.AntiForgeryToken()
                                                <button type="submit" class="btn btn-sm @(item.IsCompleted ? "btn-success" : "btn-outline-secondary")" title="@(item.IsCompleted ? "標記為未完成" : "標記為已完成")">
                                                    @if (item.IsCompleted)
                                                    {
                                                        <i class="bi bi-check-circle-fill"></i>
                                                    }
                                                    else
                                                    {
                                                        <i class="bi bi-circle"></i>
                                                    }
                                                </button>
                                            </form>
                                        </td>
                                        <td>
                                            <span class="@(item.IsCompleted ? "text-decoration-line-through text-muted" : "")">
                                                @Html.DisplayFor(modelItem => item.Title)
                                            </span>
                                        </td>
                                        <td>
                                            <span class="@(item.IsCompleted ? "text-decoration-line-through text-muted" : "")">
                                                @(string.IsNullOrWhiteSpace(item.Description) ? "-" : item.Description)
                                            </span>
                                        </td>
                                        <td>
                                            <small>@item.CreatedAt.ToString("yyyy-MM-dd HH:mm")</small>
                                            @if (item.CompletedAt.HasValue)
                                            {
                                                <br />
                                                <small class="text-success">✓ @item.CompletedAt.Value.ToString("yyyy-MM-dd HH:mm")</small>
                                            }
                                        </td>
                                        <td>
                                            <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-warning" title="編輯">
                                                <i class="bi bi-pencil"></i> 編輯
                                            </a>
                                            <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-danger" title="刪除">
                                                <i class="bi bi-trash"></i> 刪除
                                            </a>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>

                <div class="mt-3">
                    <p class="text-muted">
                        總計：<strong>@Model.Count()</strong> 個待辦事項 |
                        已完成：<strong class="text-success">@Model.Count(t => t.IsCompleted)</strong> |
                        未完成：<strong class="text-warning">@Model.Count(t => !t.IsCompleted)</strong>
                    </p>
                </div>
            }
        </div>
    </div>
</div>
